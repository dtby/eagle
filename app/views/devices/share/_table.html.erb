<!-- 通用表格复用模块（UPS系统）,单列（key: value）-->
<table id="table_<%= title %>" class="table table-bordered">
  <thead>
    <tr>
      <th colspan="2" class="text-center"><%= title %></th>
    </tr>
  </thead>
  <tbody>
    <% points.each do |k, v| %>
      <tr>
        <td width="60%" align="center"><b><%= k %></b></td>
        <td data-name="<%= k %>" align="center"><%= v %></td>
      </tr>
    <% end %>
  </tbody>
</table>

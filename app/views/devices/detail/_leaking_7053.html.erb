<div class="leaking">
  <div class="col-md-12">
    <div class="panel panel-default">
      <div class="panel-body text-center">
        <h2><%= device.name %></h2>
      </div>
    </div>
  </div>

  <div class="col-md-12 content">
    <%=image_tag "temperature.png", width: "100%", id: 'temperatureImg'%>

    <% unless exclude_points["其他"] && exclude_points["其他"].include?("漏水3")  %>
      <div class="data text-center show1">
        <i class="fa fa-circle <%= circle_color(@points["其他"]["漏水3"]) %>"></i>
        <span>漏水3</span>
      </div>
    <% end %>

    <% unless exclude_points["其他"] && exclude_points["其他"].include?("漏水2")  %>
      <div class="data text-center show2">
        <i class="fa fa-circle <%= circle_color(@points["其他"]["漏水2"]) %>"></i>
        <span>漏水2</span>
      </div>
    <% end %>

    <% unless exclude_points["其他"] && exclude_points["其他"].include?("漏水1")  %>
      <div class="data text-center show3">
        <i class="fa fa-circle <%= circle_color(@points["其他"]["漏水1"]) %>"></i>
        <div>漏水1</div>
      </div>
    <% end %>

    <% unless exclude_points["其他"] && exclude_points["其他"].include?("漏水7")  %>
      <div class="data text-center show4">
        <i class="fa fa-circle <%= circle_color(@points["其他"]["漏水7"]) %>"></i>
        <div>漏水7</div>
      </div>
    <% end %>

    <% unless exclude_points["其他"] && exclude_points["其他"].include?("漏水13")  %>
      <div class="data text-center show5">
        <i class="fa fa-circle <%= circle_color(@points["其他"]["漏水13"]) %>"></i>
        <span>漏水13</span>
      </div>
    <% end %>

    <% unless exclude_points["其他"] && exclude_points["其他"].include?("漏水6")  %>
      <div class="data text-center show6">
        <i class="fa fa-circle <%= circle_color(@points["其他"]["漏水6"]) %>"></i>
        <div>漏水6</div>
      </div>
    <% end %>

    <% unless exclude_points["其他"] && exclude_points["其他"].include?("漏水9")  %>
      <div class="data text-center show7">
        <i class="fa fa-circle <%= circle_color(@points["其他"]["漏水9"]) %>"></i>
        <span>漏水9</span>
      </div>
    <% end %>

    <% unless exclude_points["其他"] && exclude_points["其他"].include?("漏水8")  %>
      <div class="data text-center show8">
        <i class="fa fa-circle <%= circle_color(@points["其他"]["漏水8"]) %>"></i>
        <span>漏水8</span>
      </div>
    <% end %>

    <% unless exclude_points["其他"] && exclude_points["其他"].include?("漏水5")  %>
      <div class="data text-center show9">
        <i class="fa fa-circle <%= circle_color(@points["其他"]["漏水5"]) %>"></i>
        <div>漏水5</div>
      </div>
    <% end %>

    <% unless exclude_points["其他"] && exclude_points["其他"].include?("漏水4")  %>
      <div class="data text-center show10">
        <i class="fa fa-circle <%= circle_color(@points["其他"]["漏水4"]) %>"></i>
        <div>漏水4</div>
      </div>
    <% end %>

    <% unless exclude_points["其他"] && exclude_points["其他"].include?("漏水10")  %>
      <div class="data text-center show11">
        <span>漏水10</span>
        <i class="fa fa-circle <%= circle_color(@points["其他"]["漏水10"]) %>"></i>
      </div>
    <% end %>

    <% unless exclude_points["其他"] && exclude_points["其他"].include?("漏水11")  %>
      <div class="data text-center show12">
        <span>漏水11</span>
        <i class="fa fa-circle <%= circle_color(@points["其他"]["漏水11"]) %>"></i>
      </div>
    <% end %>

    <% unless exclude_points["其他"] && exclude_points["其他"].include?("漏水12")  %>
      <div class="data text-center show13">
        <i class="fa fa-circle <%= circle_color(@points["其他"]["漏水12"]) %>"></i>
        <div>漏水12</div>
      </div>
    <% end %>
  </div>
</div>


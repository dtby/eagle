<% if @attachment.errors.present? %>
	$(".modal-backdrop").remove();
	$("#attachmentModalContent").html("<%= escape_javascript( render 'attachment_modal', attachment: @attachment)%>");
	$('#attachmentModal').modal('show');
<% else %>
	window.location.href = "<%= admin_attachments_path %>"
<% end  %>